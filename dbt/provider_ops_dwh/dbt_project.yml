name: provider_ops_dwh
version: "1.0.0"
config-version: 2

profile: provider_ops_dwh

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  provider_ops_dwh:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
