# dbt workspace

This directory contains the dbt Core project and its local development profile.
Future work will add source declarations, staging models, marts, tests, seeds,
macros, and snapshots.

Credentials are read from environment variables. The checked-in defaults are
local-only lab credentials and must not be reused outside this project.
