# Python jobs

Future scripts will generate synthetic source data, extract MongoDB and CSV
records, archive raw snapshots, and load PostgreSQL raw tables.

No pipeline jobs are implemented in the initial skeleton.
